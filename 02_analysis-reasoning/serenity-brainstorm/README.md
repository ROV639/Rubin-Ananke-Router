# serenity-brainstorm

候选正式 skill 包。主入口是 `SKILL.md`，黄金范例在 `references/原始案例.md`，逐步执行文件在 `steps/`。

部署目标：

- `/Users/robin/.codex/skills/serenity-brainstorm/`
- `/Users/robin/.agents/skills/serenity-brainstorm/`
- 可选同步到 OpenClaw 对应 skills 目录，需先确认真实路径存在。

正式部署前检查：

```bash
find _workspace/skills_ready/serenity-brainstorm -maxdepth 3 -type f | sort
```

